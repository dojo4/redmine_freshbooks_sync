<%= render partial: 'freshbooks/header', locals: { name: 'projects' } %>

<%=
  render partial: 'list',
         locals: {
           projects: @projects,
           freshbooks_projects: @freshbooks_projects,
           last_synced_at: @last_synced_at,
         }
%>
